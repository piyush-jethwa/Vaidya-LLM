streamlit==1.41.1
python-dotenv==1.0.0
requests==2.31.0

rich<14

# Embeddings / RAG
chromadb==0.4.24
sentence-transformers==2.2.2
pdfplumber==0.10.3

# Speech (optional)
gtts==2.3.2

# Vision/image utilities (optional - only used if image features hit code paths)
# NOTE: if Streamlit Cloud build fails, remove opencv-python and rely on fallback encoding.
opencv-python-headless==4.10.0.84

